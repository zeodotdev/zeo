stages:
  - build
  - test
  - report

variables:
  DEFAULT_FEDORA_IMAGE: registry.gitlab.com/kicad/kicad-ci/source_containers/master/fedora:42

default:
  image:
    name: $DEFAULT_FEDORA_IMAGE
    entrypoint: ["/bin/sh", "-c"]

include:
  - local: '/.gitlab/Fedora-Linux-CI.yml'
